# 智标数据协作平台 - 测试说明

## 访问地址

```
https://time-mario-lap-topic.trycloudflare.com
```

> 公网地址，任何网络均可访问（无需同局域网）。

---

## 演示账号

| 角色 | 账号 | 密码 | 主要功能 |
|---|---|---|---|
| 甲方PM（管理员） | `admin` | `123` | 项目管理、建任务、导入明细/数据包、派发、验收、结算 |
| 甲方质检员 | `qa_01` | `123` | 质检工作台验收 |
| 供应商A团队长 | `supp_a` | `123` | 接单、内审、下载数据包、提交成果、确认结算 |
| 供应商B团队长 | `supp_b` | `123` | 同上（供应商B） |
| 标注员A1 | `anno_a1` | `123` | 标注工作台作业 |
| 标注员A2 | `anno_a2` | `123` | 标注工作台作业 |
| 算法工程师 | `algo_01` | `123` | 数据管理中心、仪表盘 |
| 数据清洗A | `clean_a1` | `123` | 数据上传+标签编辑 |

---

## 完整测试流程（闭环）

### 1. 甲方建项目并派发
- 登录 `admin` → 「项目管理」
- 新建项目 → 添加任务 → 展开任务行「导入明细」上传明细
- 任务行「导入数据包」上传 zip（供应商可下载原始数据）
- 勾选任务 → 批量派发给「供应商A」

### 2. 供应商下载数据包并标注
- 登录 `supp_a` → 「项目管理」→ 任务行「下载数据包」拿到原始数据
- 标注员 `anno_a1` → 「标注工作台」→ 领取 → 画框 → 提交

### 3. 供应商内审
- `supp_a` → 「供应商门户」/「质检工作台」→「领取质检」锁定任务 → 通过/驳回

### 4. 供应商提交成果
- `supp_a` → 提交 zip 数据包 → 等待甲方验收

### 5. 甲方验收
- `admin` / `qa_01` → 「质检工作台」验收通过
- 验收后自动生成「验收报告」+ 自动生成结算单

### 6. 结算
- 甲方「结算管理」确认 + 供应商「收款结算」确认 → 结算完成

---

## 飞书通知（自动）

| 环节 | 推送内容 |
|---|---|
| 甲方派发 | 新任务派发 |
| 供应商提交成果 | 供应商提交交付 |
| 甲方验收通过 | 验收报告（通过/返修数 + 驳回原因汇总） |
| 临近截止 | 截止提醒（距截止≤2天自动推送） |

---

## 常见问题

**打不开页面？**
- 确认服务运行中（node server/index.js）
- 确认隧道进程 cloudflared 运行中
- URL 是临时地址，隧道重启后会变，问管理员要最新地址

**供应商下载不了数据包？**
- 已修复，用带登录态的请求下载
