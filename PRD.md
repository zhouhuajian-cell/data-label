# 数据生产协同平台 PRD（Data Production Platform）

**版本**：V1.0
**更新日期**：2026-07
**适用对象**：平台使用者（甲方/供应商/标注员/数据清洗员）

---

## 1. 产品定位

本平台是一套**数据生产协同平台**，连接数据上传、数据清洗、项目派发、供应商标注、质检验收、数据集生产全流程。它不是单纯的标注工具，而是贯穿「Raw Data → Dataset」全生命周期的协作系统。

**核心流程**：
```
数据上传 → 数据清洗(打标签) → 项目绑定 → 任务拆分 → 供应商标注
→ 内部质检 → 提交 → 甲方质检 → 验收 → 生产数据集(可导出COCO)
```

---

## 2. 角色与权限

| 角色 | 账号示例 | 主要职责 |
|---|---|---|
| **甲方管理员(PM)** | `admin` / 123 | 项目管理、派发、结算审核、全局限查看 |
| **甲方质检员(QA)** | `qa_01` / 123 | 质检工作台验收、下载数据包、通过/驳回 |
| **供应商团队长(TL)** | `supp_a` / 123 | 接单、分配标注、内部质检、提交交付 |
| **标注员** | `anno_a1` / 123 | 标注工作台领取任务、Canvas标注、提交 |
| **算法工程师(R&D)** | `algo_01` / 123 | 数据上传、数据集管理、数据资产查看 |
| **数据清洗员** | `clean_a1` / 123 | 数据清洗、打标签、字段编辑 |

> 供应商质检职责已合并到供应商TL。各角色按登录账号自动跳转到对应工作台。

---

## 3. 数据管理中心（数据上传 + 清洗）

**入口**：菜单「数据管理中心」

### 3.1 数据导入
- 支持上传文件：`zip / csv / xlsx / json / 视频`
- 支持粘贴CSV文本
- 清洗模板列（自动识别）：
  ```
  源数据路径（logs）,批次,车型,宁波初筛状态,单包检测,场景,清洗人,清洗时间,感知意见
  ```
- 导入后自动生成数据集（RAW状态），每条数据自动解析为明细

### 3.2 数据清洗（打标签）
- 场景分类统计饼图（光照/天气/道路/车型/初筛等）
- 场景标签池：维度勾选（多选）
- 字段编辑：批次/车型/宁波初筛/单包检测/场景/清洗人 全部可点击选择（历史标签自动作为选项）
- 清洗时间：默认系统时间，可手动修改
- 数据路径不可修改（只读）

### 3.3 状态
- `RAW`（未清洗）→ `TAGGED`（已清洗，可被项目引用）

---

## 4. 项目管理（送标派发）

**入口**：菜单「项目管理」（PM/清洗员）

### 4.1 新建项目
- 填写项目信息（名称/类型/截止/描述）
- **绑定数据集**：选择已清洗(TAGGED)的数据集 或 上传新数据
- 任务拆分：每 N 条 = 1 个任务（自动拆分）
- 或导入Excel生成任务明细

### 4.2 任务管理
- 任务列表：状态筛选、批量派发、详情、验收
- 派发：选择供应商 + QA抽检率 + 立即开工
- 下载成果：甲方可下载供应商提交的数据包

### 4.3 数据包
- PM创建任务时可上传数据包（zip）
- 标注员可下载原始数据
- 供应商提交标注成果（zip）

---

## 5. 标注工作台（标注员）

**入口**：菜单「标注工作台」

- Canvas 2D拉框标注：拖拽画框、选择标签、编辑/删除
- 返工数据置顶（IS_REWORK优先队列）
- 作业SOP规范侧栏展示
- 防挂机工时统计（3分钟无操作暂停计时）
- 本地自动暂存（刷新不丢失）
- 动态水印（用户名+时间）
- 提交后进入供应商质检

---

## 6. 供应商门户（供应商TL）

**入口**：菜单「供应商门户」

- 任务看板：待接单/标注中/待质检/待验收/驳回/已验收（状态筛选）
- 一键接单
- 进入标注工作台
- **内部质检**：展开内嵌质检面板，逐条通过/驳回
- 提交：完成任务后提交交付给甲方

### 6.1 内部质检
- 查看标注明细（Canvas叠加框体）
- 通过 / 驳回（勾选错误分类 + 批注）
- 驳回后标注员收到返工任务

---

## 7. 质检工作台（甲方质检员）

**入口**：菜单「质检工作台」

- 左侧：待验收任务列表（仅CLIENT_QA状态）
- 选择任务 → 查看明细 → 通过/驳回
- 下载数据包（供应商提交的成果）
- 通过任意一条明细 → 任务自动验收（ACCEPTED）

---

## 8. 结算管理（PM）

**入口**：菜单「供应商管理」→「结算管理」

- 生成结算单：选择任务，自动计算
- 阶梯质量绩效系数：
  - FFR ≥ 98% → ×1.2（奖励）
  - 95% ≤ FFR < 98% → ×1.0（正常）
  - 90% ≤ FFR < 95% → ×0.8（惩罚）
  - FFR < 90% → 整包打回，不予结算
- 双方确认（甲方PM + 供应商TL）→ 结算完成
- 导出CSV对账单

---

## 9. 消息通知与飞书推送

- **站内通知**：派发/提交/驳回/验收自动生成
- **飞书Webhook推送**（PM在消息中心配置）：
  - 可配置多个飞书群
  - 事件自动推送：新任务派发、供应商提交、验收完成等
  - 手动推送测试

---

## 10. 用户管理（PM）

**入口**：菜单「用户管理」

- 添加用户：手机号/账号 + 姓名 + 密码 + 角色 + 供应商归属
- 编辑/禁用用户
- 为团队分配账号，任务按角色归属

---

## 11. 系统日志（PM）

**入口**：菜单「系统日志」

- 记录平台所有操作：项目/任务/结算/导入/治理/飞书等
- 按类型筛选、时间倒序、分页查看

---

## 12. 数据集中心（生产数据集）

生产数据集 = 验收通过的任务数据，可导出COCO格式用于模型训练。
- 数据路径/批次/车型/初筛/单包检测/场景/清洗人/清洗时间
- 标签列（场景标签）
- COCO导出（images/annotations/categories）

---

## 13. 快速上手

### 完整流程（示例）
1. **上传**：`algo_01` 登录 → 数据管理中心 → 导入CSV
2. **清洗**：`clean_a1` 登录 → 数据管理中心 → 浏览 → 打标签 → 标记已清洗(TAGGED)
3. **派发**：`admin` 登录 → 项目管理 → 新建项目 → 绑定清洗数据集 → 拆分任务 → 派发
4. **标注**：`anno_a1` 登录 → 标注工作台 → 领取任务 → 标注 → 提交
5. **内审**：`supp_a` 登录 → 供应商门户 → 内部质检 → 通过 → 提交
6. **验收**：`qa_01` 登录 → 质检工作台 → 通过 → 任务ACCEPTED
7. **产出**：生产数据集生成，COCO导出

### 常用账号
| 账号 | 密码 | 角色 |
|---|---|---|
| admin | 123 | 甲方PM |
| qa_01 | 123 | 甲方质检员 |
| supp_a | 123 | 供应商A-TL |
| anno_a1 | 123 | 标注员 |
| algo_01 | 123 | 算法工程师(R&D) |
| clean_a1 | 123 | 数据清洗员 |

---

## 14. 技术说明

- **前端**：Vue3 + Element Plus + ECharts + Canvas
- **后端**：Node.js (原生HTTP)
- **数据**：内存 + JSON文件持久化（server/data/db.json）
- **启动**：`node server/index.js`（默认端口3001）
- **构建**：`npm run build`（前端产物在dist/）

---

## 15. 状态机

**任务状态**：
```
UNASSIGNED(待指派) → ANNOTATING(标注中) → VENDOR_QA(供应商质检)
→ CLIENT_QA(甲方质检) → ACCEPTED(已验收)
                    ↘ REJECTED(驳回)
```

**明细状态**：
```
pending → annotating → annotated(待供应商质检) → vendor_passed(待甲方质检) → accepted
                                    ↘ rework(返工) → 重新提交
```
