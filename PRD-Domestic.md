# PRD-Domestic 国内GND量产数据交付流程管理系统（正式开发版）

**文档版本**：V1.0
**业务归属**：MAXIEYE 量产数据闭环 - 国内GND数据交付业务
**适用范围**：仅 Domestic 国内业务，不含海外 M57、EDR 相关业务
**交付用途**：后端开发、前端页面开发、测试验收、需求基线固化

---

## 一、业务背景

国内量产车辆持续回传智驾原始数据，原始数据无法直接用于建图、模型迭代、量产交付，需要一套标准化的数据处理链路：数据接收、质检判别、问题记录、复采处理、外协标注、成果验收、数据交付、进度统计与供应商结算。

现有业务完全依赖飞书多维表格多表联动流转，存在数据不同步、权限薄弱、人工统计繁琐、历史/增量业务混杂等问题。本次开发旨在系统化替代多维表格、固化全流程业务规则、实现自动化统计与权限隔离。

## 二、业务目标

1. 完整承接国内GND量产数据全链路业务，替代现有飞书多维表格体系。
2. 实现车端数据回传 → 质检 → 分流处理 → 验收 → 交付 → 统计结算全流程线上闭环。
3. 严格隔离在用业务与已停滞下线业务，历史可查、增量禁止流入。
4. 实现角色权限隔离，外协仅可见自有任务，内部数据严格保密。
5. 实现全流程数据留痕、可追溯、可统计、可导出。
6. 自动产出项目进度、供应商质量、交付产能统计数据，替代人工报表。

## 三、用户角色与权限职责

| 角色 | 核心职责 | 权限范围 |
|---|---|---|
| 质检工程师 | 数据接收、质检判定、问题登记、任务分流（复采/送标/废弃/直接交付） | 全部国内业务数据读写、分流操作、状态修改 |
| 复采工程师 | 处理复采任务、上传复采成果、提交验收 | 仅复采业务模块读写，不可操作标注任务 |
| 验收工程师 | 验收复采/标注成果、记录验收问题、通过/驳回判定 | 验收模块读写，不可修改上游原始任务信息 |
| 外协供应商（历帆/柏川/莱斯博/壹来倍） | 接收标注任务、执行标注、提交成果 | 仅可见自己公司任务，不可查看内部中转、质检、结算数据 |
| 项目管理员 | 查看整体进度、供应商质量、交付产能、项目风险 | 全量看板、明细只读权限 |
| 结算人员 | 导出有效结算数据、核算供应商费用 | 统计数据、结算报表只读导出 |

**备注**：烁雨、东嘉为历史外协，系统禁止新建任务，仅保留历史查询。

## 四、整体业务数据流（核心主干）

车端原始数据回传 → 系统接收入库 → 质检工程师执行质检 →

- **分支1**：质检合格 → 直接进入交付池，可交付下游
- **分支2**：质检异常 → 登记质检问题 → 人工判定分流：
  - 需要复采 → 进入复采任务流程
  - 需要标注 → 下发对应外协供应商
  - 无价值 → 标记废弃

复采/标注完成后统一提交验收 → 验收通过进入正式交付表；验收驳回退回上游重处理 → 最终交付数据进入统计看板与结算池。

### 4.1 流程图

```
A[车端回传原始数据] --> B[数据接收入库]
B --> C{质检判定}
C -->|合格| D[直接交付入库]
C -->|异常| E[登记质检问题]
E -->|复采| F[复采任务处理]
E -->|标注| G[外协标注任务]
E -->|废弃| H[任务终止]
F --> I[提交验收]
G --> I
I --> J{验收结果}
J -->|驳回| K[退回上游重处理]
J -->|通过| L[量产交付数据表]
L --> M[项目/供应商看板统计]
L --> N[结算数据统计]
```

## 五、系统核心模块构成

1. 数据接收与质检模块
2. 质检问题管理模块
3. 复采任务管理模块
4. 外协标注任务管理模块
5. 验收管理模块
6. 数据交付管理模块
7. 统计结算模块
8. 可视化看板模块
9. 权限与历史数据管理模块

## 六、全量数据状态枚举（开发直接使用）

### 6.1 质检状态
- 待质检 / 质检合格 / 质检异常 / 数据废弃

### 6.2 复采任务状态
- 待复采 / 复采处理中 / 复采已完成 / 复采废弃

### 6.3 标注任务状态
- 待下发 / 已下发 / 标注处理中 / 标注已提交 / 任务废弃

### 6.4 验收状态
- 待验收 / 验收通过 / 验收驳回

### 6.5 交付状态
- 待交付 / 已交付 / 交付作废

## 七、核心业务规则（强约束，开发必须落地）

1. 一条原始数据仅能走一条主线流程：不可同时分配复采+标注。
2. 只有质检异常数据，才允许进入复采/标注流程。
3. 只有验收通过的数据，才可进入最终交付池与结算统计。
4. 验收驳回数据必须退回上游任务节点，禁止验收环节直接修改数据内容。
5. 已停滞业务强制锁死增量：烁雨、东嘉、旧际数复采流程，仅历史可读，无法新建任务。
6. 外协账号严格隔离：只能查看、接收、提交自有公司任务，看不见其他供应商数据、看不见内部送标中转数据。
7. 所有状态变更、操作行为全员、全时间、全链路留痕日志。
8. 废弃、驳回、未验收数据不计入任何结算统计。

## 八、看板统计需求

系统需完整复刻原有多维表格看板能力，支持时间筛选、供应商筛选、数据导出：

1. 标注进度看板：各供应商任务状态分布、完成率、逾期率
2. 项目交付进度看板：接收总量、处理中、验收中、已交付总量
3. 供应商质量看板：通过率、驳回率、平均交付时效
4. 复采进度看板：复采各状态数量、复采完成产能
5. 量产产线概览：整体产能、每日增量、存量积压统计
6. 明细数据导出：支持所有业务明细 Excel 导出

## 九、历史数据与停滞业务规则

### 9.1 已下线、禁止新增业务（硬性开发约束）
- 烁雨标注任务、东嘉标注任务、旧际数复采处理流程
- 规则：页面可查询历史数据，新建任务下拉、分发逻辑彻底移除以上选项，杜绝误操作。

### 9.2 历史数据迁移规则
- 全量迁移原有 Domestic 所有历史数据
- 历史数据只读不可改，仅可查询、导出
- 新增数据走全新系统流程，与历史数据互不干扰

## 十、非功能需求

1. 可追溯：每条数据全生命周期操作日志留存（操作人、操作时间、原状态、新状态）。
2. 权限安全：内外角色隔离、供应商数据隔离、敏感内部数据隔离。
3. 性能稳定：支持大批量车端数据持续批量导入。
4. 可导出：所有明细、统计报表支持 Excel 导出。
5. 数据一致性：状态流转强联动，杜绝脏数据、状态错乱。

## 十一、上线验收标准

1. 完整跑通国内全业务链路：接收-质检-分流-复采/标注-验收-交付-统计。
2. 停滞业务无法新建任务，仅历史可查。
3. 角色权限隔离完全生效，外协数据隔离无误。
4. 所有状态流转符合业务规则，无状态冲突、无数据错乱。
5. 看板统计数据与业务明细数据完全一致。
6. 全量历史数据迁移完成、可查、只读。
7. 全流程操作日志完整可查。

## 十二、现存痛点（系统优化解决点）

1. 原有多维表格跨表联动弱、数据经常不同步 → 系统统一单源数据流转。
2. 历史/增量业务混杂易出错 → 系统强制业务隔离。
3. 权限弱、全靠飞书管控 → 系统内置精细化业务权限。
4. 结算全人工统计 → 系统自动统计、自动出报表。
